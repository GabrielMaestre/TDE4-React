import React from 'react';
import { View, Text, TextInput, Button } from 'react-native';

export default function ResultMetUnifeb(props) {
    return(
        <View>
          <Text>{props.msgResultUnifeb}</Text>
        </View>
    );
}